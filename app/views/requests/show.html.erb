<h1> Request </h1>

<%= render @request %>

<%= render 'request_options' %>

<% content_for :details do %> 
	<h3> Requested Plants </h3>

	<ul>
		<% @plants.each do |plant| %>
			<li> <%= plant.name %> </li> 
		<% end %>
	</ul>
<% end %>